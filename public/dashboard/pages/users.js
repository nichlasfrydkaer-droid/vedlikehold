import { loadDashboard } from "../services/dashboard.js";
import { getCongregation } from "../js/session.js";
import { getMembers, inviteMember, updateMember, removeMember, account } from "../js/api.js";

const safe=value=>String(value||"").replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[char]);
export async function initUsers(){
  const me=await loadDashboard(); const main=document.querySelector("main"); const congregation=getCongregation();
  if(!me?.success || !congregation){ main.innerHTML='<section class="dashboard-card dashboard-full"><h2>Brukere</h2><p>Velg en menighet først.</p></section>'; return; }
  const render=async()=>{const data=await getMembers(congregation.id); if(!data.success){main.innerHTML=`<section class="dashboard-card dashboard-full"><h2>Brukere</h2><p>${safe(data.message||"Kunne ikke hente brukere.")}</p></section>`;return;} const can=data.canManageMembers;main.innerHTML=`<section class="dashboard-card dashboard-full management-heading"><div><p class="page-eyebrow">${safe(congregation.name)}</p><h2>Brukere</h2><p>Administrer hvem som har tilgang til denne menigheten.</p></div></section><section class="dashboard-card dashboard-full member-card"><h3>Brukere med tilgang</h3><div class="member-list">${data.members.map(member=>`<article class="member-row"><div class="member-avatar">${safe(member.name).slice(0,1).toUpperCase()}</div><div class="member-main"><strong>${safe(member.name)}</strong><span>${safe(member.email)}</span></div><span class="role-pill ${member.role}">${member.role==='admin'?'Administrator':'Medlem'}</span>${data.canManageAdmins?`<select data-role="${member.id}"><option value="member" ${member.role==='member'?'selected':''}>Medlem</option><option value="admin" ${member.role==='admin'?'selected':''}>Administrator</option></select>`:''}${can?`<button class="text-danger" data-remove="${member.id}">Fjern</button>`:''}</article>`).join('')||'<p>Ingen medlemmer ennå.</p>'}</div></section>${can?`<section class="dashboard-card dashboard-full member-card"><h3>Gi en bruker tilgang</h3><p class="section-help">Eksisterende brukere får tilgang med én gang. Nye brukere mottar en invitasjon som er gyldig i 7 dager.</p><form id="inviteForm" class="inline-form"><input name="email" type="email" placeholder="navn@eksempel.no" required><select name="role">${data.canManageAdmins?'<option value="member">Medlem</option><option value="admin">Administrator</option>':'<option value="member">Medlem</option>'}</select><button type="submit">Send invitasjon</button></form><p id="memberStatus" class="form-status"></p></section>`:''}<section class="dashboard-card dashboard-full member-card"><h3>Endre adgangskode</h3><form id="passwordForm" class="password-change"><input name="currentPassword" type="password" autocomplete="current-password" placeholder="Nåværende adgangskode" required><input name="password" type="password" autocomplete="new-password" placeholder="Ny adgangskode (minst 10 tegn)" required><button type="submit">Lagre ny adgangskode</button></form><p id="passwordStatus" class="form-status"></p></section>`;
  document.querySelectorAll('[data-role]').forEach(select=>select.onchange=async()=>{const r=await updateMember(congregation.id,{user_id:Number(select.dataset.role),role:select.value});if(!r.success){alert(r.message||'Kunne ikke endre rolle.');select.value=select.value==='admin'?'member':'admin';}else render();});
  document.querySelectorAll('[data-remove]').forEach(button=>button.onclick=async()=>{if(confirm('Fjern denne brukeren fra menigheten?')){const r=await removeMember(congregation.id,Number(button.dataset.remove));if(!r.success)alert(r.message||'Kunne ikke fjerne brukeren.');else render();}});
  document.getElementById('inviteForm')?.addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const r=await inviteMember(congregation.id,{email:form.get('email'),role:form.get('role')});document.getElementById('memberStatus').textContent=r.success?(r.immediate?'Brukeren har fått tilgang.':'Invitasjonen er sendt.'):(r.message||'Kunne ikke sende invitasjon.');if(r.success){event.currentTarget.reset();render();}});
  document.getElementById('passwordForm')?.addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const r=await account('change-password',{currentPassword:form.get('currentPassword'),password:form.get('password')});document.getElementById('passwordStatus').textContent=r.success?'Adgangskoden er oppdatert.':(r.message||'Kunne ikke oppdatere adgangskoden.');if(r.success)event.currentTarget.reset();});
  }; await render();
}
